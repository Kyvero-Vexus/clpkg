# clpkg monorepo

This repository consolidates the former `clpkg-*` repositories into one tree.

## Structure

- `packages/<name>/` — one folder per former `clpkg-*` repository.

See `MIGRATION.md` for source heads and remotes at migration time.
